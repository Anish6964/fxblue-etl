steps:
  # Step 1: Build the Docker image for RSS job
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "build"
      - "-t"
      - "gcr.io/$PROJECT_ID/gcp-rss-job:latest"
      - "./gcp_rss"

  # Step 2: Push the Docker image to Google Container Registry
  - name: "gcr.io/cloud-builders/docker"
    args:
      - "push"
      - "gcr.io/$PROJECT_ID/gcp-rss-job:latest"

  # Step 3: Deploy to Cloud Run (RSS job)
  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    args:
      - "gcloud"
      - "run"
      - "deploy"
      - "gcp-rss-job"
      - "--image"
      - "gcr.io/$PROJECT_ID/gcp-rss-job:latest"
      - "--region"
      - "us-central1" # Use your region here
      - "--platform"
      - "managed"
      - "--allow-unauthenticated"
